vim.pack.add({
	"https://github.com/folke/which-key.nvim",
})

require("which-key").setup({})

-- vim.keymap.set("n", "<leader>?", require("which-key").show({ global = false }))
-- desc = "Buffer Local Keymaps (which-key)",
